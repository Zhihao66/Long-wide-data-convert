# 数据格式转换工具 - 网页版 / Data Transformer - Web Version

在线宽数据和长数据格式转换工具

## ✨ 功能

- 🔄 **宽 → 长**: 将宽格式转换为长格式
- 🔄 **长 → 宽**: 将长格式转换为宽格式
- 📁 **多格式支持**: CSV、JSON 等
- 💾 **导入/导出**: 支持文件上传和下载
- 📱 **响应式设计**: 适配桌面和移动设备
- 🚀 **无后端需求**: 纯前端实现，无需服务器

## 🚀 快速开始

### 本地运行

```bash
# 进入项目目录
cd web

# 使用 Python 内置服务器
python -m http.server 8000

# 或使用 Live Server 扩展（VS Code）
# 然后访问: http://localhost:8000
```

### 在线使用

访问: https://yourusername.github.io/data-transformer/

## 📖 使用指南

### 宽 → 长转换

1. 在输入框粘贴宽格式数据（CSV格式）
2. 选择"宽 → 长"转换模式
3. 填写 ID 列（保持不变的列）
4. 点击"开始转换"
5. 复制或下载结果

**示例输入:**
```
id,name,2020,2021,2022
1,Alice,100,120,150
2,Bob,200,210,240
```

**示例输出:**
```
id,name,variable,value
1,Alice,2020,100
1,Alice,2021,120
1,Alice,2022,150
2,Bob,2020,200
2,Bob,2021,210
2,Bob,2022,240
```

### 长 → 宽转换

1. 在输入框粘贴长格式数据
2. 选择"长 → 宽"转换模式
3. 填写索引列、列列、值列
4. 点击"开始转换"
5. 复制或下载结果

## 🎯 适用场景

### 宽格式适合：
- 数据展示和报表
- 统计分析
- Excel 表格

### 长格式适合：
- 时间序列分析
- 建立统计模型
- 数据库存储

## 📋 技术栈

- **前端**: HTML5 + CSS3 + JavaScript
- **部署**: GitHub Pages
- **无依赖**: 纯原生实现

## 🔧 文件结构

```
web/
├── index.html      # 主页面
├── app.js          # 核心逻辑
├── package.json    # 项目信息
└── README.md       # 本文件
```

## 💡 功能说明

### CSV 解析

支持标准 CSV 格式，包括：
- 逗号分隔值
- 引号转义
- 多行数据

### JSON 支持

支持直接粘贴 JSON 数组：
```json
[
  {"id": 1, "name": "Alice", "2020": 100, "2021": 120},
  {"id": 2, "name": "Bob", "2020": 200, "2021": 210}
]
```

### 文件上传

支持上传以下格式：
- `.csv` - CSV 文件
- `.json` - JSON 文件
- `.xlsx`, `.xls` - Excel 文件（需要额外处理）

## 🌐 部署到 GitHub Pages

### 步骤 1: 创建仓库

```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/yourusername/data-transformer.git
git push -u origin main
```

### 步骤 2: 启用 GitHub Pages

1. 进入仓库设置 (Settings)
2. 找到 "Pages" 部分
3. 选择 "Deploy from a branch"
4. 选择 "main" 分支，文件夹选择 "/" (根目录) 或 "/web"
5. 保存

### 步骤 3: 访问网站

- 如果 web 在根目录：`https://yourusername.github.io/data-transformer/`
- 如果 web 在子文件夹：需要调整配置

## 🔄 工作流程

```
输入数据 (CSV/JSON)
    ↓
解析为 JavaScript 对象
    ↓
选择转换模式
    ↓
应用转换逻辑
    ↓
生成输出
    ↓
展示结果 / 下载 / 复制
```

## 🐛 故障排除

### Q: 数据无法解析

A: 检查数据格式是否正确
- CSV：用逗号分隔，第一行是列名
- JSON：必须是有效的 JSON 数组

### Q: 转换结果不对

A: 确认以下几点
- ID 列名称正确
- 没有空行或特殊字符
- 数据类型一致

### Q: 无法下载文件

A: 尝试
- 检查浏览器是否允许下载
- 使用复制功能手动保存

## 🚀 进阶用法

### 大数据处理

对于超大文件（>10MB），建议：
1. 分批上传
2. 使用桌面版工具
3. 使用命令行工具

### 自定义参数

在浏览器控制台中修改：
```javascript
// 自定义转换参数
wideToLong(data, 'id,name', null, 'year', 'value')
```

## 📞 支持

- 📖 查看完整文档：[README_CN.md](../README_CN.md)
- 🐛 报告问题：[GitHub Issues](https://github.com/yourusername/data-transformer/issues)
- 💬 讨论功能：[GitHub Discussions](https://github.com/yourusername/data-transformer/discussions)

## 📄 许可证

MIT License - 详见 [LICENSE](../LICENSE)

---

**让数据转换变得简单！** 🎉
